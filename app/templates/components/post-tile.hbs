<div class="post">
  <h3 class="fancy">{{post.title}} - {{post.neighborhood}}</h3>
  <h4 class="fancy">Price: {{post.price}} || Rating: {{post.rating}}</h4>
  <img src= {{post.imgurl}}>
  <h4 class="fancy">{{post.address}}</h4>
  <p class="body">
    {{post.text}}
  </p>
  <p>
    {{#link-to 'edit' post.id id="pencil"}}<span class="glyphicon glyphicon-pencil"></span>{{/link-to}}&nbsp;<span {{action 'delete' post}} class="glyphicon glyphicon-trash" title="delete"></span>
  </p>
</div>
